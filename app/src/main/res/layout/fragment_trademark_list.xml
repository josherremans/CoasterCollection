<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorSpinnerGradientEnd"
    android:paddingBottom="@dimen/activity_vertical_margin_narrow"
    android:paddingLeft="@dimen/activity_horizontal_margin_zero"
    android:paddingRight="@dimen/activity_horizontal_margin_zero"
    android:paddingTop="@dimen/activity_vertical_margin_narrow"
    app:layout_behavior="@string/appbar_scrolling_view_behavior">

    <!--ProgressBar
        android:id="@+id/progressBar"
        android:minHeight="20dip"
        android:maxHeight="20dip"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        style="@android:style/Widget.ProgressBar.Horizontal"/-->

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:paddingLeft="5dp"
        tools:context=".MainActivity"
        android:baselineAligned="false" >

        <ListView
            android:id="@+id/lstTrademarks"
            android:layout_weight="1"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:divider="@null"
            android:dividerHeight="0px"/>

        <ScrollView
            android:layout_width="50dp"
            android:layout_height="match_parent"
            android:id="@+id/scrollView" >
            <LinearLayout
                android:id="@+id/side_index"
                android:layout_width="match_parent"
                android:layout_height="fill_parent"
                android:background="@color/colorSpinnerGradientStart"
                android:gravity="center_horizontal"
                android:orientation="vertical" >
            </LinearLayout>
        </ScrollView>
    </LinearLayout>
</RelativeLayout>
